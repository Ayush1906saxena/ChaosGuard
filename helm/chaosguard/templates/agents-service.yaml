apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaosguard.fullname" . }}-agents
  namespace: {{ include "chaosguard.namespace" . }}
  labels:
    {{- include "chaosguard.componentLabels" (dict "Root" . "Component" "agents") | nindent 4 }}
spec:
  type: {{ .Values.agents.service.type }}
  ports:
    - port: {{ .Values.agents.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaosguard.selectorLabels" (dict "Root" . "Component" "agents") | nindent 4 }}
