apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaosguard.fullname" . }}-gateway
  namespace: {{ include "chaosguard.namespace" . }}
  labels:
    {{- include "chaosguard.componentLabels" (dict "Root" . "Component" "gateway") | nindent 4 }}
spec:
  type: {{ .Values.gateway.service.type }}
  ports:
    - port: {{ .Values.gateway.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaosguard.selectorLabels" (dict "Root" . "Component" "gateway") | nindent 4 }}
